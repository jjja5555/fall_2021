package edu.northeastern.martin;

public class AbstractAndInterfaceDiff {

    /*
        1.methods:
            abstract: can have abstract and non-abstract, static or default also can
            interface: only have abstract methods

        2.variables:
            abstract: can have non-final or non-static variable, which means they have storage
            Interface:: only have static and final.

        3.  inheritence:
            abstract: can implement interface
                        can only be inheritance once
            interface: cant extend abstract
                        can be implemented multiple tiems

        access:
            interface: only public
            abstract: private, protected

     */
}
